# Railway Environment Variables Template
# Copy these to your Railway project's Variables section

# MongoDB Connection (replace with your actual MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/brainly?retryWrites=true&w=majority

# JWT Secret (generate a strong secret key)
JWT_SECRET=your_super_secure_jwt_secret_key_at_least_32_characters_long_2024

# Environment
NODE_ENV=production

# Frontend URLs (add your frontend URLs here after deployment)
FRONTEND_URL_PROD=https://your-frontend-domain.com
FRONTEND_URL_1=https://your-frontend-domain-1.com
FRONTEND_URL_2=https://your-frontend-domain-2.com

# Note: Railway automatically provides the PORT variable, so don't set it manually
